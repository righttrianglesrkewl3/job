{% extends "layout.jinja2" %}

{% block pagestyles %}

{% endblock %}


{% block content %}
  <h1>{{ body }}</h1>

  {% if current_user.is_authenticated %}

    <p>Hi {{ current_user.name }}!</p>

    <a href="{{ url_for('home_blueprint.logout') }}">Log out</a>
    
  {% endif %}

{% endblock %}
